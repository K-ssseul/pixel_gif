# 像素风 GIF 生成器 · 技术方案设计

> 一款**纯前端、零后端**的像素艺术 GIF 制作工具。所有图像处理（切图、像素化、调色板量化、GIF 编码）均在浏览器本地完成，**源文件不会上传到任何服务器**，天然满足「最大 10MB 源文件、输出分辨率 ≤500×500」的性能约束。

---

## 1. 需求映射

| 需求项 | 方案落地 |
| --- | --- |
| 支持 JPG / PNG / GIF 上传 | `<input type=file accept="image/jpeg,image/png,image/gif">` + 拖拽，FileReader 读取 |
| 切图设置：均匀网格 / 透明切割 | 两种切图引擎，纯 Canvas 像素遍历实现 |
| 生成帧列表 | 切片后输出 `ImageData[]`，缩略图网格呈现 |
| 选中需要的帧 | 帧卡片点击勾选，支持全选/全不选 |
| 像素化处理算法 | 块采样平均（block-size 可调）实现像素风 |
| 动作帧序列生成 | 多帧 GIF 经 `GifReader` 解码+帧间合成，直接作为帧序列 |
| 输出参数可调（尺寸/帧率/画质） | 输出宽高(≤500)、FPS、调色板颜色数(32/64/128/256) |
| 三步操作流程引导 | 顶部步骤条 + 面板切换状态机 |
| 实时预览 | 参数变更防抖(350ms)自动重新编码并预览 |
| 一键下载 | `Blob` + `<a download>` 触发下载 |
| 最大 10MB 源文件 | 前端 `file.size` 校验拦截 |
| 输出分辨率最高 500×500 | `clamp` 约束输出宽高上限 |

---

## 2. 技术架构

```
┌─────────────────────────────────────────────┐
│                浏览器 (纯前端)                  │
│                                               │
│  [上传] ─→ [切图引擎] ─→ [像素化] ─→ [量化] ─→ │
│     │            │            │         │     │
│   FileReader  Canvas遍历  块平均    中位切分   │
│   GIF解码                                   │
│                                              │
│  [帧列表/选帧] ──→ [GifWriter 编码] ─→ [预览/下载]│
│                        (omggif)               │
└─────────────────────────────────────────────┘
        全部在本地内存/Canvas 完成，无网络请求
```

### 2.1 关键依赖（本地 vendored，离线可用）
- **omggif**（`js/lib/omggif.js`，31KB，UMD 单文件）：同时提供
  - `GifReader`：GIF 解码（多帧提取 + 调色板索引还原 RGBA）
  - `GifWriter`：GIF 编码（索引像素 + 局部调色板 + 透明 + 循环）
- 全部图像运算依赖原生 **Canvas 2D API**，无需额外库。

### 2.2 核心算法

**像素化（块采样平均）**
对每个 `block×block` 区域取 RGBA 均值填充，区域越小越精细、越大越像素化。复杂度 O(W·H)，10MB 图亦可秒级完成。

**均匀网格切图**
`cell = floor(W/cols) × floor(H/rows)`，按行优先遍历源 `ImageData` 提取子块。

**透明切割**
统计每列/每行的不透明像素占比，低于 2% 阈值判定为「全透明分隔带」，再做连续段分割，剔除全透明子块——自动从精灵表中切出独立帧。

**调色板量化（中位切分 Median-Cut）**
1. 收集不透明像素颜色，透明像素单独标记；
2. 中位切分生成 ≤`颜色数` 的主色（采样降采样提速）；
3. 每个像素映射到最近主色索引；
4. 透明帧：调色板总长取 2 的幂 `L`，其中 `L-1` 个主色 + `1` 个透明索引槽（omggif 硬性要求调色板长度为 2 的幂）。

**GIF 编码**
- 输出缓冲 `Uint8Array(W·H·3·帧数 + 余量)`，不足时自动扩容重试；
- 每帧 `addFrame(0,0,W,H, indices, {palette, delay, transparent})`；
- `delay = round(100/FPS)`（1/100 秒）；`loop:0` 循环播放。

---

## 3. 性能与边界处理
- 源文件 `≤10MB` 上传前校验；超大尺寸图切图时已按输出尺寸重绘，内存峰值可控。
- 量化采样步长 `stride=2`（不透明像素 >20000 时）防止卡顿。
- 实时预览防抖，避免拖动滑块时高频重编码。
- 编码缓冲不足自动扩容，避免 `RangeError`。

---

## 4. 已验证的正确性
- ✅ omggif 编解码往返测试：3 帧含透明 GIF 编码→解码，透明像素正确保留；
- ✅ 纯函数单测：网格切图(4 块)、像素化(块内均匀)、透明切割(切出 2 个不透明段) 全部 PASS；
- ✅ 静态资源加载：HTML/CSS/JS/库均返回 200；
- ✅ 调色板 2 的幂约束、`0xRRGGBB` 打包格式已修正验证。

---

## 5. 已知限制 / 后续可拓展
- 单文件应用，需通过本地静态服务器或直接双击 `index.html`（file:// 下 omggif 为本地脚本，`FileReader` 仍可用，但部分浏览器对 file:// 的 Canvas 无跨域限制，一般可运行）打开。
- 画质「颜色数」选项固定为 2 的幂（32/64/128/256），符合 GIF 调色板规范。
- 可拓展：拖拽排序帧、帧级延迟、导出 APNG/WebP、批量预设。

---

## 6. 运行方式
```bash
cd pixel-gif-tool
python3 -m http.server 8080
# 浏览器打开 http://localhost:8080
```
或直接双击 `index.html`。

---

## 7. v2 增强（分组 / 自动识别 / 抠图 / 补帧 / 对齐 / 自适应预览）

在原有能力之上，v2 重点解决「多帧素材的自动理解」与「更专业的帧管理」，全部仍为**纯前端、零后端**。

### 7.1 代码结构（算法与 UI 解耦）
- `js/lib/pixel-ops.js`（UMD）：抽出全部纯算法（切图、像素化、量化、抠图、自动检测、对齐、光流、补帧），浏览器挂全局 `PixelOps`，Node 可 `require` 用于单元测试。
- `js/app.js`：仅负责 DOM / 状态机 / 事件绑定 / GIF 编码编排。

### 7.2 八项增强与对应实现

| # | 增强点 | 实现 |
| --- | --- | --- |
| 1 | **帧自动对齐** | 切图后 `analyzeSizes` 检测尺寸是否一致；不一致时弹「基准帧选择」弹窗（默认以最大帧为基准），其余帧 `fitFrameTo` 缩放/居中留边对齐到基准尺寸，保证 GIF 同画布 |
| 2 | **智能补帧** | `interpolateFrames`：`倍率补帧`(×N，每两帧插 N-1 张) / `目标帧数`(扩展到指定总帧)；算法支持 `线性混合`(前后帧插值)、`传统光流`(块匹配 warp，运动更顺滑)、`AI 补帧/RIFE 近似`(双向光流精修)。每种算法附「易懂说明」 |
| 3 | **上传自动识别帧** | 新增「自动检测」切图模式：有透明像素时按透明缝隙切分为规则网格（无透明则整图作为 1 帧）；契合「一张照片一帧 / 一张照片多帧」 |
| 4 | **自动抠背景** | `removeBackground`：仅对「无透明」的图执行，从四边 flood-fill，颜色距离 ≤ 容差视为背景→透明化，不伤害前景；已是透明 PNG 则跳过 |
| 5 | **输出默认尺寸 = 切图尺寸** | 进入第 3 步或切换分组时 `setDefaultDims` 自动把输出宽高同步为基准帧（切图格）尺寸 |
| 6 | **预览窗口自适应 GIF 大小** | `.preview-box` 改为 `width/height:auto` 包裹，`<img>` 按像素实际尺寸显示（`image-rendering:pixelated`），上限 480px 防溢出 |
| 7 | **输出画质对齐原图** | 默认调色板 256 色（原图级），输出尺寸默认 1:1 源（见 5），空间分辨率不丢 |
| 8 | **帧分组** | 每帧归属一个分组；`分组管理`工具条可新建/删除/切换分组；每帧卡片下拉框改所属分组；**每组 = 一个 GIF**，第 3 步下拉选当前分组生成，或「导出全部分组」逐个下载 |

### 7.3 关键算法补充
- **自动检测网格**：统计每列/行是否含不透明像素，连续「有内容」段即为单元格；无透明 → 单帧。
- **自动抠图**：背景色取四边采样均值；从四边入队 flood-fill，欧氏距离 ≤ 容差则置透明。
- **传统光流补帧**：对源帧分块，在相邻帧 ±R 范围内块匹配求位移向量 `(dx,dy)`；中间帧 `t` 处前向 warp 源帧、后向 warp 目标帧后平均，运动比线性混合自然。
- **AI 补帧(RIFE 近似)**：在光流基础上使用更细分块(4px)与更大搜索半径(12px)做双向精修；**真·RIFE 需加载 ONNX 模型（onnxruntime-web），离线环境用此近似实现，效果接近**。

### 7.4 验证
- ✅ `test/v2-ops.js`：16 项纯算法单测 PASS（medianCut、自动检测网格、抠图、对齐、线性/光流/目标补帧、GIF 编解码往返透明保留）；
- ✅ `debug/harness.js`：端到端流程 PASS（均匀网格生成、透明网格生成、自动检测+光流 2× 补帧 4→7 帧并生成）；
- ✅ 静态资源全部 200。

> 注：`debug/` 与 `test/` 为开发期回归脚本，不影响最终产物，可随源码保留。
